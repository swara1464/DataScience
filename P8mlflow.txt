STEP 0: CHECK AND CLEAN EXISTING PYTHON / MLFLOW
Check Python:
python3 --version

Check pip:
pip3 --version

Check MLflow:
mlflow --version

If MLflow exists but errors:
pip3 uninstall mlflow -y


STEP 1: INSTALL REQUIRED PACKAGES
Update system:
sudo apt update

Install Python tools:
sudo apt install python3 python3-pip -y

Install MLflow and ML libraries:
pip3 install mlflow scikit-learn pandas

Verify:
mlflow --version

Expected:
mlflow, version x.x.x


STEP 2: CREATE PROJECT DIRECTORY
mkdir mlflow-project
cd mlflow-project

Initialize Git:
git init
git branch -M main


STEP 3: CREATE TRAINING SCRIPT
Create file:
nano train.py

Paste exactly:
import mlflow
import mlflow.sklearn
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error
from sklearn.model_selection import train_test_split
import pandas as pd

data = {
    "x": [1, 2, 3, 4, 5],
    "y": [2, 4, 6, 8, 10]
}

df = pd.DataFrame(data)

X = df[["x"]]
y = df["y"]

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

alpha_values = [0.01, 0.1, 1.0]

for alpha in alpha_values:
    with mlflow.start_run():
        model = LinearRegression()
        model.fit(X_train, y_train)

        predictions = model.predict(X_test)
        mse = mean_squared_error(y_test, predictions)

        mlflow.log_param("alpha", alpha)
        mlflow.log_metric("mse", mse)
        mlflow.sklearn.log_model(model, "model")

        print(f"Run with alpha={alpha}, MSE={mse}")


Save and exit.


STEP 4: RUN EXPERIMENTS
python3 train.py
Expected output (example):
Run with alpha=0.01, MSE=0.0
Run with alpha=0.1, MSE=0.0
Run with alpha=1.0, MSE=0.0


This creates:

mlruns/ directory

Multiple experiment runs

STEP 5: VIEW EXPERIMENT RESULTS (OPTIONAL UI)
Start MLflow UI:
mlflow ui

Open browser:
http://127.0.0.1:5000

You will see:
Experiment runs

Parameters

Metrics

Artifacts

Stop UI with:

CTRL + C


STEP 6: IDENTIFY BEST MODEL
Lower MSE = better model.
You can say to examiner:
“The run with the lowest MSE is selected as the best-performing model.”


STEP 7: SHOW ARTIFACTS AND LOGS
List artifacts:
ls mlruns

Show Git status:
git status


STEP 8: COMMIT PROJECT FILES

git add train.py mlruns
git commit -m "Add MLflow experiment tracking"
